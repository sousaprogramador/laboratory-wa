import request from 'supertest';

import app from '../index';

describe('Laboratory', () => {
    it('should be able create a new laboratory from api', async () => {
        // const response = await request(app).post('/laboratory').send({
        //     name: 'Teste',
        //     address: 'teste',
        // });

        // console.log(teste.body);

        // const response = await request(app).get('/laboratory');

        // expect(response.status).toBe(200);
        // expect(response.body).toMatchObject(
        //     expect.objectContaining({
        //         name: 'nova tarefa',
        //         description: 'tarefa de teste',
        //         user_id: 1
        //     }),
        // );
    });
});
